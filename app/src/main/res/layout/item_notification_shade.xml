<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="4dp"
    android:layout_marginBottom="4dp"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    app:cardCornerRadius="20dp"
    app:cardBackgroundColor="?attr/colorSurfaceContainer"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="?attr/colorOutlineVariant">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="14dp"
        android:paddingTop="12dp"
        android:paddingEnd="10dp"
        android:paddingBottom="12dp"
        android:gravity="center_vertical">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/shade_item_app_icon"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:scaleType="centerCrop"
            app:shapeAppearanceOverlay="@style/ClockDesk.Shape.RoundedSmall"
            android:contentDescription="@null"
            tools:src="@drawable/ic_notifications" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            android:orientation="vertical">

            <!-- Subtitle line: APP NAME • DEVICE • TIME -->
            <TextView
                android:id="@+id/shade_item_header"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="?attr/colorPrimary"
                android:textSize="11sp"
                android:textStyle="bold"
                android:fontFamily="@font/googlesansflex"
                android:letterSpacing="0.04"
                android:textAllCaps="true"
                tools:text="WHATSAPP • PIXEL 8 • JUST NOW" />

            <!-- Title -->
            <TextView
                android:id="@+id/shade_item_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textColor="?attr/colorOnSurface"
                android:textSize="15sp"
                android:textStyle="bold"
                android:fontFamily="@font/googlesansflex"
                android:maxLines="1"
                android:ellipsize="end"
                tools:text="Alice Smith" />

            <!-- Body -->
            <TextView
                android:id="@+id/shade_item_body"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:textSize="13sp"
                android:fontFamily="@font/googlesansflex"
                android:maxLines="3"
                android:ellipsize="end"
                tools:text="Hey, are we still meeting today at 4?" />
        </LinearLayout>

        <ImageButton
            android:id="@+id/btn_dismiss_shade_item"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_close"
            app:tint="?attr/colorOnSurfaceVariant"
            android:contentDescription="@string/close" />
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
